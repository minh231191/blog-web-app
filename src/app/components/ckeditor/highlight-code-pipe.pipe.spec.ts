import { HighlightCodePipePipe } from './highlight-code-pipe.pipe';

describe('HighlightCodePipePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightCodePipePipe();
    expect(pipe).toBeTruthy();
  });
});
